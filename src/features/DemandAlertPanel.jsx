import React, { useEffect, useState } from 'react';
import { supabase } from '../../supabaseClient';
import { Bell, Search } from '../../shared/icons';
export default function DemandAlertPanel(){const [rows,setRows]=useState([]);useEffect(()=>{supabase.from('search_events').select('query,created_at').order('created_at',{ascending:false}).limit(500).then(({data})=>{const m={};(data||[]).forEach(r=>{const q=String(r.query||'').trim().toLowerCase();if(q)m[q]=(m[q]||0)+1});setRows(Object.entries(m).sort((a,b)=>b[1]-a[1]).slice(0,8))})},[]);return <div className="bg-white rounded-2xl p-4 border"><div className="flex items-center gap-2 font-bold text-sm"><Bell size={15}/> Area Demand Alerts</div><div className="text-[10px] text-gray-400 mt-1">Customers kya search kar rahe hain — future paid intelligence ka base.</div>{rows.length?rows.map(([q,c])=><div key={q} className="flex justify-between py-1.5 text-xs border-b last:border-0"><span className="flex items-center gap-1"><Search size={11}/>{q}</span><b>{c}</b></div>):<div className="text-xs text-gray-400 mt-3">Search data aana shuru hone do.</div>}</div>}
